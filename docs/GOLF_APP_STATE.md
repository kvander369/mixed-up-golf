# Golf app — living state

Written 2026-08-23, current as of **2026-08-29**. **Read this first** when
picking the golf app back up.

> **Where it stands in one line:** built, deployed, hand-checked against a real
> card, and in use. Live at v21. Seven test suites, 101 checks, all passing.
> Nothing blocking.
Design decisions live in `GOLF_PWA_PART1.md` and `GOLF_PWA_PART2_RULES.md`;
this file is where things stand and what to do next.

---

## OFFLINE VERIFIED 2026-08-23

Kyle installed it to his iPhone home screen and **confirmed it opens in airplane
mode**. The service worker is doing its job; the app needs no connection.

That was the highest-consequence unknown in the whole project and it is closed.

**2026-08-24 — the hand-check is done.** Kyle entered a card from the old
4Score/AppSheet app and the app agreed. The rules already reproduced the
Sheet's own columns (`4score_rule_verify.js`), but that was the code checking
the code; this is a human checking the code, and it passed. Nothing blocking
remains.

---

## DEPLOYED 2026-08-23 — it is live

**https://kvander369.github.io/mixed-up-golf/**

| | |
|---|---|
| Repo | `kvander369/mixed-up-golf` — public (free GitHub Pages requires it) |
| Local working copy | `C:\Users\vande\Claude\Projects\mixed-up-golf` — NOT on Drive; git and Drive fight |
| Hosting | GitHub Pages, branch `main`, folder `/ (root)` |

Verified live: all five files serve with correct content types, `viewport-fit=cover`
present, service worker registers, manifest linked, apple-touch-icon set.

### What is deployed

`index.html` is the artifact mockup wrapped in a real page — artifacts supply
their own `<head>`, so the exported file had none. `wrap.js` did that; it is not
in the repo.

Added for the PWA: `manifest.webmanifest` (name, icons, standalone display,
theme colour) and `sw.js` (cache-first service worker — the app never asks the
network once cached, which is the entire point).

Icons are Ernie composited onto the brand green `#8FD974` at 192 and 512.
Generated by PowerShell System.Drawing from `ernie.png`.

### iOS safe areas — the fix that took three goes

`viewport-fit=cover` plus `apple-mobile-web-app-status-bar-style:black-translucent`
means the web view extends **under** the status bar. Without compensation the
clock and wifi icons land on top of the COURSE / PLAYERS / NEW buttons.

**What works:** the green app bar carries the inset itself and grows upward —

```css
.appbar{
  padding-top: env(safe-area-inset-top);
  height: calc(60px + env(safe-area-inset-top));
}
```

The clock then sits on green and the buttons sit below it.

**What does NOT work — do not repeat it:** putting
`padding-top:env(safe-area-inset-top)` on `.screen`. `.screen` is
`height:100%` inside a `100dvh` stage, so padding there shifts the whole flex
column down, the tab bar swells past the bottom of the viewport, and a black
strip appears beneath it. The top overlap is not fixed either. Tried on
2026-08-23; reverted.

Bottom inset is already handled on `.tabbar` and `.shell` and needs no change.

### To change the app

Edit `index.html` in the local folder, then either push with git or upload
through the GitHub web UI. **Bump `CACHE` in `sw.js`** or installed phones keep
serving the old version from cache.

There was once a Drive copy, `golf_app_mockup.html` — the artifact-shaped
version with no `<head>`. Searched for on 2026-08-28 and not found, so the
drift risk it carried is gone. **`index.html` in this folder is the only copy.**
If one ever reappears on Drive, retire it rather than keeping two in step.

### GitHub CLI — logged in since 2026-08-24

Authenticated as `kvander369` with `repo` scope and ADMIN on the repo. The
device-flow login that was abandoned during the first deploy has since been
finished, so a change is now `git add -A && git commit -m "..." && git push`.

**Push after every commit** (rule added 2026-08-28). The remote is this
project's only backup, and to Kyle — checking the live site on his phone — an
unpushed commit is indistinguishable from a change that was never made.

Note for next time: the `!` prefix in Claude Code runs **Bash**, not PowerShell —
`& "C:\Program Files\..."` is a syntax error there. Use
`"/c/Program Files/GitHub CLI/gh.exe"`.

---

## What exists right now

These are the real filenames. The mockup-era names (`golf_app_mockup.html`,
`golf_app_smoke.js`) are gone — it became a deployed app and the files were
renamed with it.

| File | What it is |
|---|---|
| `index.html` | **The app.** One self-contained file, no build step, no dependencies. Runs offline as-is. |
| `sw.js` | Cache-first service worker. `CACHE` is the only string an installed phone compares |
| `manifest.webmanifest` | Name, icons, standalone display, theme colour |
| `icon-192.png`, `icon-512.png` | PWA icons — Ernie on brand green `#8FD974` |
| `smoke.js` | Renders every screen and fires every control against a DOM stub — 19 checks |
| `live_test.js` | Change one score, everything downstream updates — 5 checks |
| `skins_test.js` | Gross only, birdie-or-better, ties knock out — 9 checks |
| `pops_separation_test.js` | Guards the two different stroke allocations (see below) — 7 checks |
| `4score_rule_verify.js` | Decoded 4Score rules reproduce the real Sheet's own columns — 9 checks |
| `roster_test.js` | The roster keeps its promises — 27 checks |
| `nassau_test.js` | The Nassau settles the way the group plays it; runs the app's real function — 25 checks |
| `CLAUDE.md` | Read first, every session |
| `RESTORE.md` | What a clone does NOT bring back, and where the data actually lives |

Four files are **gitignored on purpose** and a fresh clone will not have them:
`RESUME-Claude.bat`, `seat-check.ps1`, `icon.png`, `mixedupgolf.ico`.
`RESTORE.md` says how to get each one back.

Run all seven suites after any change to `index.html`. As of 2026-08-29 they are
101 checks and all green.

**Nothing has a build step.** Open Question 2 in `GOLF_PWA_PART1.md` is answered:
this is one HTML file, so it can live in Drive with everything else and no local
`node_modules` folder is needed.

---

## Structure as built

**Top of screen = set once, before the round:** `Course` (Pars / Rankings
sub-tabs) · `Players` · `New`.

**Bottom tabs = during and after:** `Holes` · `Scores` · `Results`.

Kyle's principle, 2026-08-23: *"Buttons at the top are pre-round info. Buttons at
the bottom are during and/or after the round use."*

| Screen | Purpose |
|---|---|
| **Course** | 18 pars + 18 hole rankings, entered in 9-hole chunks on a 3x3 pad |
| **Players** | 8 names + course handicaps, typed. Team 2 blank = a four-player round |
| **Holes** | Team 1's foursome only. The inside game, live. Tap any hole to enter it |
| **Hole entry** | One hole, one screen: 4 scores on the keypad, junk grid, greenie steppers |
| **Scores** | Player-by-player nine entry — the after-the-round path, and where team 2 goes |
| **Results** | Progress while incomplete; team-vs-team 2-best-ball-net and skins when done |

**Holes and Scores share one store.** Both read and write `state.scores`; there
is no sync step and nothing can drift.

---

## The two things most likely to be broken by a future tidy-up

### 1. The two games allocate strokes DIFFERENTLY

- **Inside game** (Holes): pops = course handicap **− low man in the foursome**
- **CCW 8-player + random draw**: **raw course handicap**, nothing subtracted

Same formula, different input — which is exactly why sharing one function looks
right and is silently wrong. Guarded by `pops_separation_test.js`.
Full detail in `GOLF_PWA_PART2_RULES.md` §2b.

### 2. Only gross scores reach the screen

Net is computed to colour holes and never rendered per-hole. The one net figure
on screen is the team total on Results, labelled as net. See §4b of the rules doc.

---

## Players is home plate

Kyle, 2026-08-23: *"Players is in essence the home page. Home plate if you will."*

**Every New Round lands on Players**, whatever option was chosen. The demo is the
only exception — it is already complete, so it opens on Holes.

This is not hand-holding. The roster sets who the low man is; the low man sets
everyone's pops; pops decide every hole colour and every net result. A name in
the wrong slot scores the entire round wrong and **nothing on screen looks
broken**. It has to be right before anything else means anything.

It also genuinely changes every round: the group re-throws balls to remix the
foursomes each time they play, so slot order is never the same twice. Kyle is
always slot 1.

**Slot order is load-bearing:** slots 1–4 are Kyle's foursome, and within it
1&2 play 3&4 in the inside game. Slots 5–8 are team 2; leaving them blank makes
it a four-player round.

**Opening behaviour:** first ever run opens on Players. Every run after that
reopens on the exact screen the app was left on, with the full round restored —
scores, junk, roster, course and the draw. Nothing ever clears itself; only
New Round clears anything.

---

## The demo round is a permanent feature — do not strip it

Kyle, 2026-08-23: *"we should probably keep it for testing and for showing off
the app to mates."*

`New Round > Fill a demo round` loads a complete 18 holes for eight players plus
junk. It is the only way to reach Results, the draw and the inside game without
typing eight scorecards, and it is how the app gets demonstrated.

Team 1's card is the **real CCW sample** from `CCW_System_README.md` §2.1, so
what appears on screen is honest data, not invented numbers.

It is also the one exception to landing on Players: its roster is already
complete, so it opens on Holes. The rule is "land on Players whenever the roster
needs attention", and here it does not.

---

## Ernie

Kyle's own cartoon engineer, from his class notes — his logo. Sits at the bottom
of the Players page. Kyle: *"Its kinda my logo."*

Source is `ernie.ico` from `C:\Users\vande\Claude\Projects\Yale Email Triage V3`
(found via the icon on the Email Triage desktop shortcuts). A clean 256x256 PNG
frame was extracted from it and saved here as `ernie.png`. It is a better copy
than the older `sb_mascot.png`, which has a black plate behind it.

**Embedded as a data URI**, not linked — the app has to work with no connection.
Done by script and byte-verified; the archive records an earlier attempt that
failed by pasting 15K of base64 through an editor.

**Apply no CSS filter to him.** Decoding the pixels settled what two guesses did
not: the artwork is a **white fill with black outlines on a transparent
background** (of 16384 sampled pixels, 11616 transparent; of the opaque ones,
3146 near-white and 1193 dark). He therefore already reads as a white Ernie on
the dark ground. `invert(1)` does nothing useful, and `brightness(0) invert(1)`
paints the fill and the linework the same white — a featureless blob.

Sized 58px wide, cropped to 55px tall to trim the long neck stroke to a stub.

---

## Decisions made 2026-08-23 that are easy to accidentally undo

- **No holes-won ticker.** Kyle plays 5 ways with automatic presses; a count of
  holes won says nothing about who owes what. Removed on his instruction.
- **No Nassau logic, no presses.** The app colours holes; Kyle does the money.
- **Junk is hand-entered.** No rules to derive.
- **Junk counters show the ROUND TOTAL**, carried across holes; tapping adds one
  booked against the current hole (small green `+n` in the corner). At 18 the
  grid shows the whole round's junk at a glance.
- **Scores above 9 cannot be entered.** Deliberate.
- **Pops markers are never green or gold** — white clipped corner for one shot,
  pink for two. Green/gold mean who won the hole and nothing else.
- **App starts empty — but the PEOPLE do not.** Since the roster (2026-08-24)
  the group lives in its own storage key and survives "New round — clear
  everything". The ROUND still starts empty. See the roster section below.
- **First-time reassurances stripped.** Kyle: *"if the app works, which it will,
  no need to tell me every letter is saved."*

---

## CLOSED 2026-08-23 — the demo-round bug

**Kyle reported "New Round › Fill a demo round" did nothing in his browser.**
Fixed the same day, and the app has been in real use since. It is kept here
because the cause is the standing hazard of this codebase, not a one-off.

Two real bugs of this class were already found and fixed today, both caused by
**over-matching regexes during refactors**:

1. `initials()` deleted in the CSS-Grid rewrite but still called → Holes threw.
2. `renderHoleEntry()` swallowed whole by the Pops-removal regex → tapping a
   hole did nothing.

Lesson recorded, and it is rule 1 of `CLAUDE.md`: **never edit this file with
broad regex replacements.** Two functions and nearly 10KB were destroyed that
way, and both survived a syntax check to fail at runtime. Targeted edits only,
and run the suites after every change.

This has since bitten twice more in the same shape — a port script that nearly
published the real roster because its end-anchor swept up too much, and two test
suites that quietly stopped testing anything. Every edit script in this project
now fails loudly unless its pattern matches exactly once.

The build carries an on-screen red error bar (`#errbar`) plus `window.onerror`,
so a failure reports its own message instead of dying silently. That is what
turned this from a guessing game into a fix, and it stays in.

Also added: a guard that resets `state.step` to `holes` if a saved round names a
screen that no longer exists — the removed Pops tab was a candidate for exactly
this, and would have produced a blank screen on load.

---

## STATUS CHANGE 2026-08-23 — this REPLACED 4Score, and it did

Kyle: *"I will probably never use the 4Score app again and just use this. It's
much nicer."*

That moved the goalposts at the time: no longer a mockup that has to look right,
but software a round of golf and real money depends on. It set two conditions,
**both since met**:

1. **Offline.** DONE — installed to the home screen and confirmed opening in
   airplane mode, 2026-08-23.
2. **One real hand-check.** DONE 2026-08-24 — Kyle entered a card from the old
   4Score/AppSheet app and the two agreed. That is a human checking the code,
   not the code checking the code.

---

## The roster — added 2026-08-24

Setting up used to be eight typed fields every round. Now it is four taps.
**ROSTER** sits on the top bar beside Course and Players; the full rules are in
`CLAUDE.md`, and `roster_test.js` guards them with 27 checks. The three that
carry weight:

1. **One CH per person, and it is the last one you used.** No stored-versus-
   today's handicap to reconcile and no save step — nothing can go stale
   because nothing is kept in two places. This matters more than it looks: a
   wrong handicap moves the low man, and the low man sets every pop in the
   inside game.
2. **People live in their own key** (`mixedUpGolf.people.v1`), not inside the
   round. "New round — clear everything" wipes scores and never the people.
3. **Names on the card must be distinct.** Two men in the group share a first
   name, so one carries an initial on the card. Scoring never cared (it is all
   by slot index) but the roster keyed on name, so the second man could not
   exist. A duplicate is now refused with a reason. Their real names are
   deliberately not recorded here — see the note in `CLAUDE.md`.

**The shipped roster is EMPTY, on purpose.** `index.html` is published publicly,
so seeding it would put eleven men's names and handicaps into a public repo
permanently. `defaultPeople()` returns `[]` and must stay that way. Kyle builds
the list on the phone.

---

## Screen work — 2026-08-24 to 2026-08-27

All from Kyle using it on a real phone, in a real round.

- **Won holes read at a glance.** The winning cell now takes a green or gold
  wash, not just coloured type. The pops tint that used to sit behind it was
  fighting that colour and is gone — but the **corner flags stay** (white clip
  for one shot, pink for two). They were dimmed and shrunk after Kyle:
  *"the white chits in the corner really end up lighting up the screen"*.
- **Hole entry got taller.** Four across, first name stacked above the score so
  each cell is a rectangle not a square, with a divider between the names and
  the keypad. Team 1 on the LEFT in green, team 2 on the RIGHT in gold.
- **`HE_CELLS` is a named constant, and that is deliberate.** A 2x2 grid filled
  in plain order silently put one man from each side in every column while the
  colours said otherwise. Kyle caught it by eye. Naming the order stopped it
  coming back.
- **The handicap left the Holes header.** Kyle: *"one can count the chits."*
- **Zoom is off entirely.** Fast score entry double-taps the same key — golf
  scores repeat — which iOS reads as zoom. Killed with `touch-action`,
  `maximum-scale=1`, and swallowed `gesturestart/change/end`. Also
  `user-select:none` to stop the text loupe.

**Adding a head `<script>` for that broke two test suites silently.** Both took
script `[1]` as the app and everything-before-the-first-script as the markup, so
they found ZERO controls and passed while testing nothing. They now use
`pageParts()`: longest script block is the app, markup is everything that is not
a script. Worth remembering — a test that passes by testing nothing is worse
than one that fails.

---

## The version stamp — added 2026-08-28 (v12), made readable (v13, v14)

Small line at the bottom of the Players screen showing which version the phone
is running. Kyle: *"how do I know I am on version 11"* — and before this the
only answers were curling the live site or hunting for a visual tell.

**Where the number comes from is the whole design.** It is NOT a constant in
`index.html`. That would be a second copy to keep in step with `sw.js`, and a
stamp that can be wrong is worse than no stamp — it would say v14 while the
phone served v13, which is the exact confusion it exists to end. Instead the
page asks the service worker that is serving it (`postMessage("version")`) and
the worker answers out of its own `CACHE` string. One string, one source.

- It asks **once, at boot**, so the number describes the worker that served
  THIS page load. A phone that picks up a new version in the background keeps
  showing the old number until reopened — correct, because the old version is
  what is still on screen.
- **"not installed"** means no service worker has taken over yet: first ever
  load, or opened as a plain file. That is an answer, not an error.
- `smoke.js` asserts it renders and is not blank. That assertion was verified
  by deliberately breaking the stamp and watching the test fail — this project
  has already shipped two suites that passed while testing nothing.

**It took three goes to make it visible,** which is the lesson worth keeping:
11px in `#5A5A5A` on a near-black ground is invisible on a phone in daylight,
and `--ink-2` at 15px was not much better. It is now plain white at 20px. A
line you consult once a week has to be legible when you consult it.

---

## 2026-08-29 — a real round, then a day of changes (v15 → v21)

Kyle played a round on v14 and came back with a list. `v14-known-good` is
tagged on GitHub as the way back, and that restore was tested (fresh clone at
the tag, all suites run in it) before anything changed.

**The hole screen, made legible (v15–v17).** The junk grid was `#5A5A5A` at
16px - the version-stamp mistake again. Kyle: *"I don't want to have to put in
my readers to enter in the scores."* White and bigger throughout; the keypad,
which had been shrunk to 41px purely so the screen fit without scrolling, is
58px - he would rather scroll than squint. The pop marker on the hole cells
became the same corner flag the Holes grid uses (grey-white for one, pink for
two) after he caught me shipping dots: *"we need to be consistent."* Junk and
greenie counts wear their side's green or gold.

**The Nassau (v18–v21).** Kyle: *"you have something in results for the Inside
Game but that's not really what we play."* Rules in `CLAUDE.md`; the rule
book is `nassau_test.js`. Built as a pure function over the per-hole result,
tested before the screen existed, then the screen shaped by Kyle looking at it
in a browser: presses aligned under the hole they launched from, no labels on
press rows, money per nine beside its name, no grey section headers, dollar
signs on the stakes, the carry switch only when the front is actually tied.
The old "take it 5–4" block on Results is gone - *"the new game trumps all
that"* - and the Holes grid's Out/In show the bets on that nine ("4-2-0")
instead of holes won.

**Things checked and found not to be bugs:** the "no skins" report (skins are
gross-only, by Kyle's own 2026-08-23 rule - a net birdie is not a skin); the
top bar (New ends 8px inside a 393px frame; the rounded corner only looks like
clipping); the pop chits vanishing (my test card had everyone scratch).

**Three lessons for the record.** The browser check matters: none of the
suites can see CSS, and twice today a broken comment passed every test. Kyle
reads without glasses, outdoors - when he says something is hard to see, go
decisively bigger and white; nudging costs a deploy each time. And a doc edit
script that uses `String.replace` with a plain string doubled this file: a
dollar sign followed by a backtick in the new text means "insert everything
before the match". Pass the replacement as a function.

---

## Still open

**One thing, and it is a slow-burn unknown, not a bug.**

**iOS storage eviction.** Home-screen PWAs are *believed* exempt from Safari's
roughly 7-day eviction of site data, but this has never been verified here. If
that belief is wrong, the app may open after a winter off with the roster gone.
The consequence is bounded — retype eleven names, and `RESTORE.md` says so
outright — but it would be a nasty surprise in a clubhouse. The honest test is
time: check it after a long gap and record the answer here.

Everything else on the old "what is left" list is done: service worker and
manifest, hosting, `viewport-fit=cover`, and the hand-check.
